# Requirements file for ragwire package.
# Install with: pip install -r requirements.txt
#
# Floors mirror pyproject.toml. They are the versions RAGWire is tested against,
# not aspirational minimums. langchain-core <1.0 in particular resolves fine
# but fails at runtime (AIMessage.text was a method there, not a property).

# Core dependencies
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
langchain-community>=0.4.0,<1.0.0
langchain-text-splitters>=1.0.0,<2.0.0

# Vector database (qdrant-client >=1.12 for the facet API)
qdrant-client>=1.12.0,<2.0.0
langchain-qdrant>=1.0.0,<2.0.0

# Document processing
markitdown[pdf]>=0.1.0,<1.0.0

# Page-wise splitting (splitter.strategy: page)
pypdf>=4.0.0,<7.0.0
python-pptx>=1.0.0,<2.0.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0,<3.0.0

# Progress bars
tqdm>=4.66.0

# Optional: OpenAI support
langchain-openai>=1.0.0,<2.0.0

# Optional: HuggingFace support
langchain-huggingface>=1.0.0,<2.0.0

# Optional: Ollama support
langchain-ollama>=1.0.0,<2.0.0

# Optional: Google/Gemini support
langchain-google-genai>=3.0.0,<5.0.0

# Optional: FastEmbed for sparse vectors
fastembed>=0.4.0,<1.0.0

# Optional: Groq LLM
langchain-groq>=1.0.0,<2.0.0

# Optional: Anthropic LLM
langchain-anthropic>=1.0.0,<2.0.0

# Optional: OpenRouter support for the LLM (ChatOpenRouter) and embeddings (openrouter SDK)
# NOTE: langchain-openrouter requires Python >= 3.10
langchain-openrouter>=0.2.0,<1.0.0
openrouter>=0.9.0,<1.0.0
